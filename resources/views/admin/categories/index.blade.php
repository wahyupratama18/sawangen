<x-app-layout>
    <div class="p-8">
        <div class="flex justify-end mb-3">
            <a href="{{ route('admin.categories.create') }}">
                <x-jet-button>
                    {{ __('basic.create') }}
                </x-jet-button>
            </a>
        </div>

        isine nang kene
    </div>
</x-app-layout>
